import React from 'react';
import {BaseTemplate, AgencyList} from 'components';

const AgencyListPage = () => {
    return (
        <BaseTemplate>
            <AgencyList/>
        </BaseTemplate>
    );
};

export default AgencyListPage;
